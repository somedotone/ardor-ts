// import { Passphrase, Account } from './index'
import { Account, Passphrase, account } from './index' 


// const passphrase = Passphrase.generate();
// const pubKey = Account.convertPassphraseToPublicKey(passphrase) as string;

// console.log(passphrase);
// console.log(Account.convertPublicKeyToAccountRs(pubKey));

const test = new Account();
const test2 = new Passphrase();